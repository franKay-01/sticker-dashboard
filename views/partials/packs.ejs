<!--Display Collections from parse-->
<%if ( collections.length === 0 ){%>
<div class="medium-3 large-3 cell icons">
    <p class="none">No Packs</p>
    <div class="name_tag"><p class="pack_name_text">No Packs</p></div>

</div>
<%}else {%>
<% collections.forEach(function(item, index) { %>

<div class="medium-3 large-3 cell icons">
    <a href="/pack/<%= item.id %>">
        <% if (item.get("art_work").url() === "undefined" || item.get("art_work").url() === undefined){ %>
        <center>
        <p class="none">No Artwork</p>
        </center>
        <div><p class="pack_name_text"><%= item.get("pack_name") %></p></div>
        <% } else {%>
            <center>
                <img src="<%= item.get("art_work").url() %>" style="height: 250px">
            </center>
            <div class="name_tag"><p class="pack_name_text"><%= item.get("pack_name") %></p></div>
        <%}%>
    </a>

</div>

<% }); %>
<%}%>